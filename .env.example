# StrideGuide Environment Configuration
# Copy this file to .env and update values as needed

# =============================================================================
# SUPABASE CONFIGURATION (Optional Cloud Features)
# =============================================================================

# Supabase project URL - used for optional cloud features only
# Core guidance, finder, and SOS work completely offline
VITE_SUPABASE_URL="https://your-project.supabase.co"

# Supabase anonymous key - SAFE to expose in client-side code
# This is a PUBLIC key with limited permissions defined by RLS policies
# Never use the service role key in client applications
VITE_SUPABASE_ANON_KEY="your-anon-key-here"

# =============================================================================
# FEATURE FLAGS (Development/Production Control)
# =============================================================================

# Enable cloud AI descriptions (user can still disable in settings)
# Default: false (fully offline operation)
VITE_CLOUD_DESCRIBE_ENABLED="false"

# Enable telemetry collection (user must still opt-in)
# Default: false (no data collection)
VITE_TELEMETRY_ENABLED="false"

# Development mode features
# Default: false (production optimizations)
VITE_DEV_MODE="false"

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# CSP nonce for dynamic scripts (generated at build time)
# Automatically handled by Vite build process
# VITE_CSP_NONCE="auto-generated"

# Build environment (affects console logging and debug features)
NODE_ENV="production"

# =============================================================================
# ACCESSIBILITY SETTINGS
# =============================================================================

# Default language (user can change in app)
VITE_DEFAULT_LANGUAGE="en"

# High contrast mode by default
VITE_HIGH_CONTRAST_DEFAULT="false"

# Large text mode by default  
VITE_LARGE_TEXT_DEFAULT="false"

# =============================================================================
# PERFORMANCE TUNING
# =============================================================================

# Low-end device mode (reduces performance requirements)
VITE_LOW_END_DEVICE_MODE="false"

# Winter mode (enhanced battery saving for cold weather)
VITE_WINTER_MODE="false"

# Target FPS for camera processing
VITE_TARGET_FPS="30"

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================

# ✅ SAFE TO INCLUDE:
# - VITE_SUPABASE_URL (project URL)
# - VITE_SUPABASE_ANON_KEY (public anonymous key)
# - VITE_* prefixed variables (exposed to client)

# ❌ NEVER INCLUDE:
# - Supabase service role key (server-side only)
# - Private API keys
# - Database passwords
# - JWT secrets

# 🔒 RLS SECURITY:
# The Supabase anon key is safe to expose because:
# - Row Level Security (RLS) policies control data access
# - Default deny policies prevent unauthorized access
# - Only explicitly allowed operations are permitted
# - All sensitive operations require user authentication

# 📱 OFFLINE-FIRST DESIGN:
# StrideGuide works completely offline by default:
# - Camera processing: 100% local (never uploaded)
# - Emergency contacts: encrypted local storage only
# - SOS functionality: uses device SMS (no internet required)
# - Lost item finder: local ML processing only
# - Supabase used only for optional feedback and updates

# 🛡️ PRIVACY BY DESIGN:
# - No user accounts required
# - No personal data on servers by default
# - User controls all data collection
# - Complete local data deletion available